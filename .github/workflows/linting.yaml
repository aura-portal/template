---
name: Linting
on:
  push: null
permissions:
  contents: read
jobs:
  linting:
    permissions:
      contents: read
      packages: read
      statuses: write
      checks: write
    uses: aura-portal/.github/.github/workflows/linting.yaml@main
